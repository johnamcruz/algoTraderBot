# logs
log/
# python
__pycache__/
*.pyc
venv/
# derived cache (regenerated by precompute_proba.py / train_ppo_exit.py)
proba_cache.npz
# secrets
.env
# macOS
.DS_Store

# 1-min bar data is >100MB/file — too large for GitHub; keep local only
data/*_1min.csv
