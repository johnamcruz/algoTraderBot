numpy>=1.24
pandas>=2.0
requests>=2.28
xgboost>=2.0
joblib>=1.3
python-dotenv>=1.0       # load TopstepX credentials from .env
# Model library (public GitHub). Provides the Chronos embedding (embed_bars),
# the joblib head classes the bundles unpickle into, and the SuperTrend/ATR/ADX
# primitives the strategies reuse. Pulls torch + chronos-forecasting transitively.
futures-foundation-model @ git+https://github.com/johnamcruz/Futures-Foundation-Model.git
# PPO trailing exit (training only — the live bot runs the policy in pure numpy).
gymnasium>=0.29
stable-baselines3>=2.0
# Tests (pytest tests/) — unit + e2e, no network/broker/Chronos needed.
pytest>=7.0
# Exit-config search (optimize_exit.py) — Optuna scan over ACTIVATE_R/GIVEBACK_R/STOP_ATR.
optuna>=3.0
